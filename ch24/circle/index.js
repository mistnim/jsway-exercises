const circle = require("./circle.js");

console.log(
  `The circumference of a circle of radius 3 is ${circle.circumference(3)}`
);

console.log(`The area of a circle of radius 3 is ${circle.area(3)}`);
